<%= link_to user_path(user) do %>
  <% if user.avatar.present? %>
    <%= image_tag user.avatar.url, class: 'avatar' %>
  <% else %>
  	<i class="avatar avatar-color-<%= user.id % 215 %>"><%= user.name[0] %></i>
  <% end %>
<% end %>